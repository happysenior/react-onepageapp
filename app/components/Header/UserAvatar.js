import styled from 'styled-components';

import NormalImg from 'components/Img';

const UserAvatar = styled(NormalImg)`
  width: 36px;
  height: 36px;
  border-radius: 50%;
  display: inline-flex;
`;

export default UserAvatar;
